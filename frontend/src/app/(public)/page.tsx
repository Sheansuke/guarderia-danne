export default async function Home() {
  // const response = await getChildByCodeUseCase({ code: "F2mru9NYgLJ95Q74FzM7pKS"});
  // const response = await getReportByCodeUseCase({ code: "F2mru9NYgLJ95Q74FzM7pKS", childEndpoint: ENDPOINTS.CHILD_ENDPOINT});
  // const response = await getPaymentByCodeUseCase({ code: "F2mru9NYgLJ95Q74FzM7pKS", childEndpoint: ENDPOINTS.CHILD_ENDPOINT});
  // const response = await getReviewsUseCase();


  return (
    <main>
      <h1>GUARDERIA DE DANNE</h1>
    </main>
  );
}
